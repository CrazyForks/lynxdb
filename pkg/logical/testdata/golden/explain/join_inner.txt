Let $right
  1. Project(user_id, name)
    2. Scan(users)
      columns: [_raw, _source, _time, name, user_id]

3. Join(inner, on=[user_id])  [spill-capable]
  4. Scan(events)
  Right:
    5. Project(user_id, name)
      6. Scan(users)

Annotations:
  Optimizer rules:
    column-pruning (1x)
