# file: backend_server.log
# format: json
from main
| extend has_error = if(exists(error), "yes", "no")
| extend has_amount = if(exists(amount_cents), "yes", "no")
| stats count() by has_error, has_amount
| sort has_error, has_amount
