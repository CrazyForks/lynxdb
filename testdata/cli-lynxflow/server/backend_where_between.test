# format: json
# language: lynxflow
from idx_backend
| where (duration_ms >= 20 and duration_ms <= 6000)
| stats count(), avg(duration_ms) as avg_dur, min(duration_ms) as min_dur, max(duration_ms) as max_dur