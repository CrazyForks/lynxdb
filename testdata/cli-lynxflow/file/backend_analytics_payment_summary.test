# file: backend_server.log
# format: json
# language: lynxflow
from main
| where service == "payment-service"
| stats count() as total, count(where status == 500) as failures, avg(duration_ms) as avg_dur