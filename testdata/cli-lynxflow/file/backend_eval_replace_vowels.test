# file: backend_server.log
# format: json
# language: lynxflow
from main
| extend clean = replace(message, "[aeiou]", "*")
| extend has_star = if(contains(clean, "*"), "yes", "no")
| stats count() by has_star