# file: backend_server.log
# format: json
from main
| extend msg_upper = upper(message)
| extend msg_len = len(msg_upper)
| extend msg_short = substr(msg_upper, 0, 10)
| stats count(), avg(msg_len) as avg_len
| extend avg_short = len(msg_short)
