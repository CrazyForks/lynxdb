# file: backend_server.log
# format: json
# language: lynxflow
from main
| extend severity = case(level == "ERROR", "critical", level == "WARN", "warning", 1 == 1, "normal")
| stats count() by severity
| sort severity